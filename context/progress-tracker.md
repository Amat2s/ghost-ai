# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 29: Complete

## Current Goal

- Feature 29: Spec UI integration

## Completed

- **01-design-system**: shadcn/ui installed and configured (Tailwind v4 detected), 7 UI components added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, `lib/utils.ts` with `cn()` helper created, dark theme CSS variables and `@theme inline` token mappings wired into `globals.css`. TypeScript check passes with zero errors.
- **02-editor**: `components/editor/editor-navbar.tsx` (fixed top navbar, sidebar toggle with `PanelLeftOpen`/`PanelLeftClose`, dark bg + bottom border) and `components/editor/project-sidebar.tsx` (floating overlay, slides from left, `Projects` header + close button, My Projects / Shared tabs with empty states, full-width New Project button). Dialog pattern from Feature 01 already supports title/description/footer actions. TypeScript check passes with zero errors.
- **03-auth**: `@clerk/nextjs` v7 + `@clerk/ui` v1.14.0 installed. `proxy.ts` at root uses `clerkMiddleware` + `createRouteMatcher` to protect all routes except `/sign-in(.*)` and `/sign-up(.*)`. `ClerkProvider` wraps root layout with `ui={ui}` from `@clerk/ui` and `appearance={{ theme: dark, variables: { ... } }}` — CSS variable overrides for all colors, no hardcoded hex. `/sign-in/[[...sign-in]]` and `/sign-up/[[...sign-up]]` pages use two-panel layout (left: logo + tagline + feature list; right: Clerk form; small screens: form only). `/` redirects authenticated users to `/editor`, unauthenticated to `/sign-in`. `app/editor/page.tsx` created as the protected editor workspace. `UserButton` added to editor navbar right section. `npm run build` passes.
- **04-project-dialogs**: Editor home screen with heading, description, and New Project button. `lib/mock-projects.ts` — `MockProject` interface + 3 mock entries (2 owned, 1 shared). `hooks/use-project-dialogs.ts` — dedicated hook managing dialog type, selected project, create/rename form state, and loading state. `components/editor/project-dialogs.tsx` — `CreateProjectDialog` (name input + live slug preview), `RenameProjectDialog` (prefilled input, Enter submits, autofocus), `DeleteProjectDialog` (destructive confirm, no input). `ProjectSidebar` updated with project items, hover-reveal rename/delete actions for owned projects only, shared tab shows items without actions, mobile backdrop scrim. TypeScript check passes with zero errors.
- **05-prisma**: `prisma/models/project.prisma` — `ProjectStatus` enum (`DRAFT`/`ARCHIVED`), `Project` model (id, ownerId, name, optional description, status, optional canvasJsonPath, timestamps; indexes on ownerId and createdAt), `ProjectCollaborator` model (id, projectId, email, createdAt; cascade delete relation; unique on projectId/email; indexes on email and projectId/createdAt). `lib/prisma.ts` — cached `PrismaClient` singleton on `globalThis` in development; branches on `DATABASE_URL`: `prisma+postgres://` → `{ accelerateUrl }`, otherwise → `PrismaPg` adapter. Migration `20260530020346_add_projects` applied. Client generated to `app/generated/prisma/`. `npm run build` passes with zero errors.
- **06-project-apis**: `app/api/projects/route.ts` — `GET` lists all projects owned by the authenticated user (ordered by `createdAt` desc); `POST` creates a project with the Clerk user ID as `ownerId`, defaulting name to `Untitled Project`. `app/api/projects/[projectId]/route.ts` — `PATCH` renames a project (owner-only, 403 otherwise); `DELETE` deletes a project (owner-only, 403 otherwise). Both files return 401 for unauthenticated requests and 404 when the project doesn't exist. UI not wired. `npm run build` passes with zero errors.
- **07-wire-editor-home**: `lib/projects.ts` — `ProjectData` interface (`id`, `name`) + `getOwnedProjects` / `getSharedProjects` server helpers (Prisma select, Clerk `auth()` / `currentUser()` for email). `hooks/use-project-actions.ts` — replaces `use-project-dialogs.ts`; manages dialog state + real API mutations (`POST`, `PATCH`, `DELETE`); slug + short suffix generate `roomIdPreview` for the create dialog; `handleCreate` navigates to `/editor/[id]`; `handleRename` calls `router.refresh()`; `handleDelete` redirects to `/editor` if deleting the active workspace (derived from `usePathname()`), otherwise refreshes. `components/editor/editor-home-client.tsx` — client shell holding sidebar toggle + dialog state; receives `ownedProjects`/`sharedProjects` as props from the server component. `app/editor/page.tsx` — converted to async server component; fetches owned and shared projects in parallel and passes them to `EditorHomeClient`. `components/editor/project-sidebar.tsx` and `project-dialogs.tsx` updated to use `ProjectData` instead of `MockProject`; create dialog accepts `roomIdPreview` prop. `npm run build` passes with zero errors.
- **08-editor-workspace-shell**: `lib/project-access.ts` — `getProjectWithAccess(projectId)` server helper; resolves Clerk `userId` + primary email, queries Prisma for owner or collaborator match, returns `{ project, isOwner }` or `null`. `components/editor/access-denied.tsx` — centered lock icon, short message, link back to `/editor`. `app/editor/[roomId]/page.tsx` — async server component; redirects unauthenticated users to `/sign-in`, shows `AccessDenied` for missing or unauthorized projects, fetches owned/shared projects in parallel and renders `WorkspaceClient`. `components/editor/workspace-client.tsx` — client shell with sidebar + AI sidebar toggle state, `EditorNavbar` (project name + share + AI toggle), `ProjectSidebar` (active room highlighted), canvas placeholder, collapsible right AI sidebar placeholder, full dialog set from `useProjectActions`. `components/editor/editor-navbar.tsx` extended with optional `projectName`, `isAiSidebarOpen`, `onToggleAiSidebar` props — adds Share button and right panel toggle for workspace context. `components/editor/project-sidebar.tsx` extended with optional `activeProjectId` prop — highlights the active project in both owned and shared tabs. `npm run build` passes with zero errors.
- **09-share-dialog**: `app/api/projects/[projectId]/collaborators/route.ts` — `GET` lists owner + collaborators enriched with Clerk display name + avatar (parallel `getUser` for owner and `getUserList` for collaborators); `POST` invites by email (owner only, upsert); `DELETE` removes by email from body (owner only). `components/editor/share-dialog.tsx` — client dialog; title "Share project" with description; workspace link card with Copy link button; email input with mail icon prefix and branded Invite button (owner only); "People with access" list with total count; each person in a rounded card — owner shows OWNER badge (brand teal), collaborators show COLLABORATOR badge (muted) with trash remove button; `CollaboratorAvatar` renders Clerk image or initials fallback. `components/editor/editor-navbar.tsx` extended with `onShare` prop wired to the Share button. `components/editor/workspace-client.tsx` — `isOwner` prop added; `isShareOpen` state drives `ShareDialog`. `app/editor/[roomId]/page.tsx` — passes `access.isOwner` to `WorkspaceClient`. `npm run build` passes with zero errors.
- **10-liveblocks-setup**: `@liveblocks/node` installed (v3.19.3, was absent despite client packages being present). `liveblocks.config.ts` — `Presence` typed with `cursor: { x: number; y: number } | null` and `isThinking: boolean`; `UserMeta.info` typed with `name`, `avatar`, `color`. `lib/liveblocks.ts` — lazy cached `Liveblocks` node client on `globalThis.__liveblocks` (deferred to avoid constructor validation at build time); `getUserCursorColor(userId)` deterministically maps a user ID to one of 8 hex palette colors via a hash. `app/api/liveblocks-auth/route.ts` — `POST` requires Clerk auth (401 if missing), reads `room` from JSON body, calls `getProjectWithAccess` (403 + forbidden response if denied), calls `getLiveblocks().getOrCreateRoom` to ensure the room exists, issues an access-token session via `prepareSession` with `name`/`avatar`/`color` user info, grants `FULL_ACCESS` to the room, and returns the authorized token. `LIVEBLOCKS_SECRET_KEY` placeholder added to `.env.local`. `npm run build` passes with zero errors.
- **11-base-canvas**: `types/canvas.ts` — `CanvasNodeData` interface (label, color, shape), `CanvasNode` / `CanvasEdge` generic Node/Edge types with `"canvasNode"` / `"canvasEdge"` type literals, `NODE_COLORS` (8 fill+text pairs from ui-context), `NODE_SHAPES` (6 shape names), `NodeShape` union type. `components/editor/canvas.tsx` — client component using `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true })` from `@liveblocks/react-flow`; renders `ReactFlow` with `ConnectionMode.Loose`, `fitView`, dot-pattern `Background`, and `MiniMap`. `components/editor/canvas-wrapper.tsx` — client wrapper with `LiveblocksProvider` (authEndpoint `/api/liveblocks-auth`), `RoomProvider` (room ID + `initialPresence: { cursor: null, isThinking: false }`), inline `ErrorBoundary`, and `ClientSideSuspense`. `components/editor/workspace-client.tsx` — canvas placeholder replaced with `<CanvasWrapper roomId={project.id} />`. `@xyflow/react/dist/style.css` imported in `app/layout.tsx`. `npm run build` passes with zero errors.
- **12-shape-panel**: `components/editor/canvas-node.tsx` — `CanvasNodeComponent` registered as the `"canvasNode"` node type; renders every shape as a bordered rectangle with label centered (shape-specific visuals deferred), handles at all four sides. `components/editor/shape-panel.tsx` — floating pill toolbar at bottom-center of canvas; 6 draggable shape buttons (rectangle, diamond, circle, pill, cylinder, hexagon) each encoding `{ shape, width, height }` in the `"application/ghost-shape"` drag transfer; default sizes follow the spec (rectangle wider than tall, circle square, diamond slightly larger). `components/editor/canvas.tsx` — `nodeTypes` registered; `onDragOver` + `onDrop` handlers added to `ReactFlow`; drop reads the payload, converts screen position via `screenToFlowPosition`, and calls `onNodesChange([{ type: "add", item: newNode }])`; node IDs generated from `shape-timestamp-counter`; `ShapePanel` rendered via React Flow `Panel` at `"bottom-center"`. `components/editor/canvas-wrapper.tsx` — wrapped `Canvas` with `ReactFlowProvider` so `useReactFlow()` in `Canvas` has the shared store context. `npm run build` passes with zero errors.
- **refactor-slug-removal**: Removed `slug` from the `Project` model entirely — the `id` (cuid) is now the canonical identifier used in all URLs (`/editor/[roomId]`). Migration `20260601000000_remove_project_slug` drops the column and unique index. `lib/slug.ts`, `lib/mock-projects.ts`, and `hooks/use-project-dialogs.ts` deleted. `ProjectData` no longer has a `slug` field. `getProjectWithAccess` queries by `id`. All sidebar hrefs, share-dialog copy-link, and post-create navigation use `project.id`. `roomIdPreview` removed from `CreateProjectDialog`. `npm run build` passes with zero errors.

- **13-node-shape**: `components/editor/canvas-node.tsx` — replaces placeholder renderer with proper per-shape rendering: rectangle/pill/circle use CSS `border` + border-radius (rounded-xl / rounded-full); diamond and hexagon use inline SVG `<polygon>` with `preserveAspectRatio="none"` and `vectorEffect="non-scaling-stroke"`; cylinder uses SVG `<path>` (body) + `<ellipse>` (top cap). Border color driven by `selected` prop: `#2a2a30` at rest, `#00c8d4` when selected. Label centered via `absolute inset-0 flex items-center justify-center` for SVG shapes. `components/editor/shape-panel.tsx` — adds shape drag preview: `DragButton` hides native drag image via `setDragImage` on a 1px offscreen div, then tracks cursor via `onDrag` events and lifts position state to `ShapePanel`; a `ShapeGhost` component renders each shape using the same CSS/SVG logic as the node renderer, at full default size with brand-cyan border and 60% opacity; preview is portaled to `document.body` with `position:fixed` to escape React Flow's stacking context. `npm run build` passes with zero errors.

- **14-node-editing**: `components/editor/canvas-node.tsx` — adds `NodeResizer` (from `@xyflow/react`) to all six shape branches: visible only when `selected`, minimum 80×40, subtle dark handle (`#1a1a20` fill, `#00c8d4` stroke) with 1px brand-cyan border line. Adds inline label editing: `useReactFlow().updateNodeData(id, { label })` commits changes to the collaborative state; local `isEditing` + `editValue` state drives a shared `NodeLabel` sub-component that renders either a centered `<span>` (with `opacity-40` placeholder when empty, `onDoubleClick` to enter edit mode) or a `<textarea>` (`autoFocus`, `nodrag nopan`, `onMouseDown` stops propagation, `onBlur` commits, `Escape` cancels). `npm run build` passes with zero errors.

- **15-nodes-color-toolbar**: `components/editor/canvas-node.tsx` — adds a `ColorToolbar` component rendered via `NodeToolbar` (from `@xyflow/react`) positioned 8px above every selected node. Shows all 8 `NODE_COLORS` pairs as circular swatches. Active swatch has the pair's text color as its border plus a matching `outline` ring. Hovering a non-active swatch applies a tight `box-shadow` glow in the pair's text color. Swatch `onMouseDown` stops propagation (prevents node drag / canvas pan) and calls `updateNodeData(id, { color: fill })`, which updates the node background through the existing collaborative state — no server calls. The `pair` lookup with `NODE_COLORS[0]` fallback ensures new nodes start active on the neutral-dark swatch. `npm run build` passes with zero errors.

- **16-edge-behaviour**: `types/canvas.ts` — `CanvasEdgeData` interface added (`label?: string`); `CanvasEdge` updated to use it. `components/editor/canvas-edge.tsx` — custom `CanvasEdgeComponent` using `getSmoothStepPath` for right-angle routing with `borderRadius: 8`; renders a transparent 16px-wide hit path (hover/double-click target) over a 1.5px visible `BaseEdge`; edge stroke transitions between dimmed `rgba(180,180,195,0.35)` at rest and `rgba(180,180,195,0.85)` on hover/select; `EdgeLabelRenderer` positions the label at the `getSmoothStepPath` midpoint coordinates; label editing on double-click (input sized to content, saves on blur/Enter, cancels on Escape); saved labels shown as pill badges; faint "label" hint shown when active edge has no label; all mouse events on the label prevent canvas drag/pan. `components/editor/canvas-node.tsx` — handles refactored to use `HANDLE_CLASS` (2.5px white dot, dark border, hidden by default, `group-hover:opacity-100`); all shape branches consolidated into an inner `ShapeBody` function; node root is now `<div className="group relative h-full w-full">` so group-hover applies to handles. `components/editor/canvas.tsx` — `edgeTypes` registered with `canvasEdge: CanvasEdgeComponent`; `defaultEdgeOptions` sets `type: "canvasEdge"` and `markerEnd: { type: MarkerType.ArrowClosed, color: rgba(180,180,195,0.35), width/height: 16 }`. `npm run build` passes with zero errors.

- **17-canvas-ergonomics**: `hooks/useKeyboardShortcuts.ts` — new hook; receives a `FlowInstance` (zoomIn/zoomOut) plus undo/redo handlers; listens on `window` keydown; skips inputs, textareas, and contentEditable targets; supports `+`/`=` zoom in, `-` zoom out, `Cmd/Ctrl+Z` undo, `Cmd/Ctrl+Shift+Z` and `Cmd/Ctrl+Y` redo. `components/editor/canvas.tsx` — `MiniMap` removed; `ControlBar` component added: pill-shaped, bottom-left `Panel`, two groups (zoom out / fit view / zoom in + thin divider + undo / redo) using lucide-react icons; zoom actions call `useReactFlow()` methods with 300ms duration; undo/redo wired to `useUndo()`/`useRedo()` from `@liveblocks/react`; disabled buttons get `opacity-30 pointer-events-none` via Tailwind `disabled:` variants; `useCanUndo()`/`useCanRedo()` drive the disabled state; `useKeyboardShortcuts` called in `Canvas` with the flow instance and undo/redo handlers. `npm run build` passes with zero errors.

- **18-starter-template**: `components/editor/starter-templates.ts` — `CanvasTemplate` interface (`id`, `name`, `description`, `nodes`, `edges`) and `CANVAS_TEMPLATES` array with three templates: Microservices (API gateway → auth/user/product services → caches/DBs), CI/CD Pipeline (source → build → test → quality gate → deploy → production + artifact store), Event-Driven System (two producers → event bus → three consumers + dead letter queue); all nodes use shared `CanvasNode`/`CanvasEdge` types and `NODE_COLORS` palette; each edge includes `type: "canvasEdge"` and `markerEnd` so they render correctly when added programmatically. `components/editor/starter-templates-modal.tsx` — Dialog with scrollable 3-column grid of template cards; each card renders an SVG diagram preview (`TemplateDiagramPreview`) — calculates `viewBox` from node bounds with padding, draws edges as `<line>` elements between node centers, draws nodes as proper per-shape SVG elements (`rect`, `polygon`, `ellipse`, etc.) with fill/stroke from their color data and text labels; Import button calls `onImport(template)` then closes the dialog. `components/editor/canvas.tsx` — adds `pendingTemplate`/`onTemplateDone` props; `useEffect` on `pendingTemplate` reads current nodes/edges via a `stateRef`, removes all existing nodes/edges, adds template nodes/edges, calls `fitView` after 80 ms, then calls `onTemplateDone`. `components/editor/canvas-wrapper.tsx` — passes `pendingTemplate` and `onTemplateDone` through to `Canvas`. `components/editor/editor-navbar.tsx` — optional `onOpenTemplates` prop; renders a `LayoutTemplate` icon + "Templates" button in the workspace toolbar beside Share. `components/editor/workspace-client.tsx` — `isTemplatesOpen` + `pendingTemplate` state; Templates button opens the modal; `onImport` sets `pendingTemplate`; `onTemplateDone` resets it to `null`. `npm run build` passes with zero errors.

- **19-presence-avatars-cursors**: `liveblocks.config.ts` — renamed `isThinking` to `thinking` in the `Presence` type per spec. `components/editor/canvas-wrapper.tsx` — updated `initialPresence` to match. `components/editor/presence-avatars.tsx` — new component using `useOthers()` + `useAuth()`; filters collaborators to exclude the current Clerk user; renders up to 5 overlapping avatar circles with photo/initials fallback, a +N overflow chip, and a divider that only appears when at least one collaborator exists; always renders the Clerk `UserButton` for the current user; displayed via React Flow `Panel position="top-right"` inside the canvas. `components/editor/live-cursors.tsx` — new component using `useOthers()` + `useViewport()`; converts each participant's flow-space cursor to canvas-container-space using `flowX * zoom + vpX`; renders a colored SVG pointer and name badge per participant; renders only others (never self). `components/editor/canvas.tsx` — imports `useUpdateMyPresence`; `onMouseMove` calls `screenToFlowPosition` and broadcasts cursor; `onMouseLeave` clears cursor to null; `LiveCursors` and `PresenceAvatars` rendered inside `<ReactFlow>`. `npm run build` passes with zero errors.

- **20-ai-sidebar-shell**: `components/editor/ai-sidebar.tsx` — new component; fixed-position, slides in from the right (`translate-x-full` → `translate-x-0`) with `duration-200 ease-in-out`, floats above the canvas without pushing content; accepts `isOpen` and `onClose` props; `inert`/`aria-hidden` pattern mirrors `ProjectSidebar`. Header has bot icon in `bg-ai/20` container, "AI Workspace" title (`text-copy-primary`), "Collaborate with Ghost AI" subtitle (`text-copy-muted`), and close button. Two-tab layout (shadcn `Tabs`): **AI Architect** — scrollable chat area with empty state (bot icon + description + three starter chips as `bg-subtle text-ai-text` pills), user messages right-aligned (`bg-accent-dim border-2 border-brand/50 text-copy-primary`), assistant messages left-aligned (`bg-elevated border-surface-border text-ai-text`), auto-resizing textarea (72px min / 160px max), `Enter` submits / `Shift+Enter` newline, send button (`bg-ai text-white`); **Specs** — Generate Spec button (`bg-ai text-white`) and a static demo spec card (`bg-elevated border-surface-border`) with file icon, title, snippet, and disabled Download action. `components/editor/workspace-client.tsx` — inline `aside` placeholder replaced with `<AiSidebar>` (always rendered, visibility driven by `isOpen`); canvas no longer shares row with the sidebar. `npm run build` passes with zero errors.

- **21-canvas-autosave**: `@vercel/blob` installed. `prisma/models/project.prisma` — `canvasJsonPath` field renamed to `canvasBlobUrl` (`@map("canvas_blob_url")`); migration `20260603043257_rename_canvas_blob_url` applied. `app/api/projects/[projectId]/canvas/route.ts` — `PUT` receives canvas JSON, uploads to Vercel Blob at `canvas/{projectId}.json` (public, no random suffix), stores the returned URL on the Prisma project record; `GET` reads the blob URL from Prisma and proxies the JSON back to the client; both routes use a typed `resolveAccess` helper that checks owner or collaborator access. `hooks/use-canvas-autosave.ts` — watches `nodes` and `edges`, skips the initial render, debounces saves by 2 s, calls `PUT /api/projects/[projectId]/canvas`, exposes `SaveStatus` (`idle | saving | saved | error`). `components/editor/canvas.tsx` — `projectId` prop added; autosave hook wired; initial-load effect runs once on mount: if the room is empty (no nodes/edges), it fetches `GET /api/projects/[projectId]/canvas` and applies the saved nodes/edges; if the room already has content the load is skipped entirely; save status changes propagated via `onSaveStatusChange` callback. `components/editor/canvas-wrapper.tsx` — threads `projectId` and `onSaveStatusChange` through to `Canvas`. `components/editor/workspace-client.tsx` — `saveStatus` state added, `setSaveStatus` passed to `CanvasWrapper`, forwarded to `EditorNavbar`. `components/editor/editor-navbar.tsx` — `saveStatus` prop added; `SaveStatusIndicator` component renders next to the project name with icons and labels for saving (spinning loader), saved (teal check), and error (red alert) states; idle state renders nothing. `npm run build` passes with zero errors.

- **22-design-agent-api**: `prisma/models/task-run.prisma` — `TaskRun` model (`id`, `runId` unique, `projectId`, `userId`, `createdAt`; indexes on `runId` and `[userId, projectId]`); migration `20260603061417_add_task_runs` applied; client regenerated. `trigger/design-agent.ts` — `designAgent` task (`id: "design-agent"`, payload `{ prompt, roomId }`), logs input, returns `{ roomId, prompt, status: "received" }`. `app/api/ai/design/route.ts` — `POST` validates auth and body fields, checks owner-only project access, triggers `design-agent` via `tasks.trigger`, creates a `TaskRun` record, returns `{ runId }`. `app/api/ai/design/token/route.ts` — `POST` validates auth and body `runId`, looks up `TaskRun` and verifies ownership, calls `auth.createPublicToken` scoped to that run with 1 h expiry, returns `{ token }`. No AI calls, no canvas mutations. `npm run build` passes with zero errors.

- **23-design-agent-logic**: `trigger/design-agent.ts` — full AI implementation using Gemini (`gemini-2.0-flash` via `@ai-sdk/google`) and `generateObject` with a Zod discriminated-union schema covering all 7 operation types (add_node, move_node, resize_node, update_node_data, delete_node, add_edge, delete_edge); reads current canvas state from `lb.getStorageDocument` for context; applies operations directly to Liveblocks storage via `lb.mutateStorage` (initializes the `"flow"` LiveObject if the room is empty); broadcasts AI status events (`AI_STARTED`, `AI_STATUS`, `AI_COMPLETED`, `AI_ERROR`) via `lb.broadcastEvent`. `liveblocks.config.ts` — `RoomEvent` updated with the four typed AI event variants. `components/editor/canvas.tsx` — `useEventListener` added; local `aiStatus` state drives a floating `Panel position="top-center"` chip showing the current AI message with an animated pulse dot; auto-clears on completion or after 5 s on error; `onAiEvent` callback prop added and threaded up through `canvas-wrapper.tsx`. `components/editor/ai-sidebar.tsx` — accepts `projectId` prop; `handleSend` POSTs to `/api/ai/design`, then polls `/api/ai/design/token` + Trigger.dev run status until terminal state; chat shows user and assistant messages with live status bubbles (pulse dot while running, error styling on failure). `components/editor/workspace-client.tsx` — passes `project.id` to `AiSidebar`. `npm run build` passes with zero errors.

- **24-ai-presence-state**: `types/tasks.ts` — new file; `aiStatusFeedMessageSchema` (Zod, optional `text` field) and `AiStatusFeedMessage` type for validating feed payloads. `components/editor/workspace-client.tsx` — `LiveblocksProvider` + `RoomProvider` lifted up to wrap the entire workspace (canvas + AI sidebar), replacing the per-`CanvasWrapper` providers; both components now share the same room context. `components/editor/canvas-wrapper.tsx` — removed `LiveblocksProvider` and `RoomProvider`; retains `ErrorBoundary`, `ClientSideSuspense`, `ReactFlowProvider`, and `Canvas`. `components/editor/ai-sidebar.tsx` — `useUpdateMyPresence` sets `thinking: true/false` on generation start/end; `useCreateFeed`/`useCreateFeedMessage` post status updates to the `ai-status-feed` Liveblocks feed at each polling step; `useFeedMessages("ai-status-feed")` subscribes to the latest message (validated through `aiStatusFeedMessageSchema`), shown as a status bar below the header when AI is active; `useOthers` detects collaborators with `thinking: true` presence; header pulse dot and status bar appear when `anyoneThinking` is true; send button renders a spinning `Loader2` icon while `isGenerating`. `components/editor/live-cursors.tsx` — `CursorMarker` accepts a `thinking` prop; when `true`, a small inline SVG spinner renders inside the name badge before the name text; hidden when `thinking` is false or absent. `npm run build` passes with zero errors.

- **25-sidebar-chat-feed**: `types/tasks.ts` — `aiChatFeedMessageSchema` (Zod schema: `sender` string, `role` enum `["user","system"]`, `content` string, `timestamp` number) and `AiChatFeedMessage` type added. `components/editor/ai-sidebar.tsx` — `AI_CHAT_FEED = "ai-chat"` constant added, kept separate from `AI_STATUS_FEED`; `useSelf()` added to get current user's display name as sender; `useFeedMessages("ai-chat")` subscribes to the chat feed; incoming messages validated with `aiChatFeedMessageSchema` before rendering (invalid messages silently dropped); tabs updated from 2-column to 3-column grid with new "Chat" tab between "Architect" and "Specs"; Chat tab renders messages in order with sender name, formatted timestamp (`hh:mm`), and content — current user's messages right-aligned with brand styling, others left-aligned; empty state shows a `MessageSquare` icon and description; `handleChatSend` creates the feed if absent then posts a validated message, clears input on success, shows inline error text on failure; `isSendingChat` drives a spinner on the send button and disables textarea during send; `Enter` submits, `Shift+Enter` inserts newline; chat scroll auto-scrolls to bottom on new messages. `npm run build` passes with zero errors.

- **26-ai-chat-functional**: `app/api/ai/design/route.ts` — after triggering the task and creating the `TaskRun`, creates a public Trigger.dev token scoped to the run and returns `{ runId, publicToken }` in one response (no second round-trip required). `components/editor/ai-sidebar.tsx` — polling loop removed entirely; `useRealtimeRun(runId, { accessToken: publicToken, enabled: !!runId && !!publicToken })` from `@trigger.dev/react-hooks` tracks run status; `runId`/`publicToken` stored in state, set after a successful `/api/ai/design` call; `useEffect` on `realtimeRun?.status` drives message updates and state cleanup for all terminal/intermediate statuses; `handleSend` now pushes the user prompt to `ai-chat` feed (via `postToAiChatFeed`) before triggering the run so it's visible to all collaborators; AI completion and error messages also post to `ai-chat`; status strip moved from global below-header position into the Architect tab, rendered between the scroll area and the input area, visible only when `isGenerating`; user chat bubbles updated from teal-brand to `bg-success text-base` (green) in both Architect and Chat tabs; submit buttons updated to `bg-success text-base` with `disabled:opacity-40`; global shared-feed status bar removed. `npm run build` passes with zero errors.

- **27-spec-generation-flow**: `trigger/generate-spec.ts` — replaced stub with `schemaTask` using Zod validation; accepts `projectId`, `roomId`, `chatHistory`, `nodes`, `edges`; calls `gpt-4o-mini` via OpenRouter + `@ai-sdk/openai`'s `generateText`; updates run metadata at `starting`/`generating`/`completed` stages; returns `{ spec: text }` as Markdown. `app/api/ai/spec/route.ts` — `POST` validates body with Zod, authenticates user, resolves project access from `roomId` via `getProjectWithAccess` (no client-supplied `projectId`), triggers `generate-spec` task, creates `TaskRun` record, returns `{ runId }`. `app/api/ai/spec/token/route.ts` — `POST` validates auth + `runId`, verifies `TaskRun` ownership, issues 1h public Trigger.dev token scoped to that run. TypeScript and build pass.

- **28-spec-persistence-download**: `prisma/models/project-spec.prisma` — `ProjectSpec` model (`id` cuid, `projectId` FK with cascade, `filePath` blob URL, `createdAt`; index on `projectId`); `Project` relation `specs ProjectSpec[]` added; migration `20260604110621_add_project_specs` applied; client regenerated. `trigger/generate-spec.ts` — after `generateText`, uploads Markdown to Vercel Blob at `specs/{projectId}/{ctx.run.id}.md` (private, no random suffix), creates `ProjectSpec` record with the blob URL. `app/api/projects/[projectId]/specs/[specId]/download/route.ts` — `GET` authenticates user, verifies project access (owner or collaborator), verifies the spec's `projectId` matches the route param, fetches the blob using `get()`, returns the Markdown as a `text/markdown` attachment. TypeScript and build pass.

- **29-spec-ui-integration**: `react-markdown` installed. `app/api/projects/[projectId]/specs/route.ts` — `GET` authenticates user, verifies project access, returns all `ProjectSpec` records for the project (`id`, `createdAt`, `filePath`) ordered by `createdAt` desc. `components/editor/ai-sidebar.tsx` — Specs tab fully wired: fetches spec list from the new endpoint on sidebar open; displays each spec with filename (extracted from `filePath`) and formatted `createdAt` timestamp; per-item download button (hover-reveal); clicking a spec opens a preview Dialog; preview fetches content from the existing download endpoint via `fetch()` and renders it as Markdown using `react-markdown` inside a `ScrollArea`; modal includes its own Download button; Escape/close resets state. TypeScript and build pass.

## In Progress

- None.

## Next Up

- (TBD)



## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Tailwind v4 CSS-based configuration: all theme tokens defined in `globals.css` via `@theme inline` — no `tailwind.config.js/ts` needed.
- Dark-only app: shadcn CSS variables (`--background`, `--foreground`, etc.) are set directly in `:root` to the dark theme values from `ui-context.md`. No `.dark` class toggling required.
- Project token naming: CSS vars (`--bg-base`, `--text-primary`, etc.) are mapped to Tailwind color tokens via `@theme inline` (e.g. `--color-base: var(--bg-base)`) to produce utilities `bg-base`, `text-copy-primary`, `border-surface-border`, `text-brand`, etc.

## Session Notes

- Next.js 16 with Tailwind v4. Shadcn components in `components/ui/` must not be modified after generation.
- shadcn CLI version 4.8.2 was used.
- Next.js 16 renames middleware to **Proxy** — use `proxy.ts` (not `middleware.ts`) at the project root. Export the function as `default` or named `proxy`.
- `@clerk/ui` v1.14.0 provides `dark` theme from `@clerk/ui/themes`. Pass `ui={ui}` to `ClerkProvider` and use `appearance={{ theme: dark, variables: { ... } }}` (NOT `baseTheme` — that's the old @clerk/themes API).
- Prisma 7 uses `provider = "prisma-client"` generator (not `prisma-client-js`). Generated output at `app/generated/prisma/`; main import is `app/generated/prisma/client.ts` (no `index.ts`). Constructor requires an argument: `{ adapter }` for direct connections, `{ accelerateUrl }` for Accelerate.
- `prisma.config.ts` at project root uses `schema: "prisma/"` for multi-file schema discovery — no `--schema` flag needed on CLI commands.
- `prisma migrate dev` does NOT auto-run `prisma generate` in v7 — run both separately.
